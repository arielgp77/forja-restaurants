export function HeroBanner({
  title,
  description,
  imageUrl,
  primaryHref,
  secondaryHref,
}: {
  title: string;
  description: string;
  imageUrl: string;
  primaryHref: string;
  secondaryHref: string;
}) {
  return (
    <section className="relative h-[75vh] w-full overflow-hidden">
      
      <img
        src={imageUrl}
        alt="hero"
        className="absolute inset-0 h-full w-full object-cover scale-110"
      />

      <div className="absolute inset-0 bg-black/55 backdrop-blur-[1px]" />

      <div className="relative z-10 flex h-full flex-col justify-center px-6 md:px-16 text-white">
        
        <div className="mb-4">
  <img src="/assets/positanos/logo.jpg" className="h-20 w-20 rounded-full border-4 border-white shadow-xl" />
</div>
<h1 className="text-4xl md:text-6xl font-black leading-tight">
          {title}
        </h1>

        <p className="mt-4 max-w-xl text-lg text-white/85">
          {description}
        </p>

        <div className="mt-6 flex gap-4">
          <a
            href={primaryHref}
            className="rounded-full bg-red-600 px-6 py-3 font-bold shadow-lg hover:bg-red-700 transition"
          >
            Order Now
          </a>

          <a
            href={secondaryHref}
            className="rounded-full border border-white/40 px-6 py-3 font-bold backdrop-blur-md hover:bg-white/10 transition"
          >
            View Menu
          </a>
        </div>

      </div>
    </section>
  );
}

